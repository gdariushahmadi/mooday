# Mooday marketing & app
# - Landing is fully indexable in both EN and AR.
# - /app is a client-only SPA shell (Cold-launch Welcome + Discover) and
#   should not be indexed as bare HTML — links with `?view=...` parameters
#   would otherwise bloat the index with every possible deep-link variant.

User-agent: *
Allow: /
Allow: /?lang=ar
Disallow: /app

Sitemap: /sitemap.xml
